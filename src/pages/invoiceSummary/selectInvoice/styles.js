import { StyleSheet} from "react-native";
import { colors } from "../../../themes";


export const styles = StyleSheet.create({
    expired: {
        fontSize: 16,
        fontWeight: "medium",
        paddingRight: 12.5,
        paddingLeft: 12.5
    },
});