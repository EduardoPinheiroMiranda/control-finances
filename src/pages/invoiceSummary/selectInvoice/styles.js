import { StyleSheet} from "react-native";


export const styles = StyleSheet.create({
    expired: {
        width: 85,
        textAlign: "center",
        fontSize: 16,
        fontWeight: "medium",
        marginHorizontal: 12.5,
    },
});